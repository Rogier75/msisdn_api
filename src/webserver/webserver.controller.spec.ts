import { Test, TestingModule } from '@nestjs/testing';
import { WebserverController } from './webserver.controller';
import { WebserverService } from './webserver.service';

describe('AppController', () => {
  // let appController: WebserverController;
  //
  // beforeEach(async () => {
  //   const app: TestingModule = await Test.createTestingModule({
  //     controllers: [WebserverController],
  //     providers: [WebserverService],
  //   }).compile();
  //
  //   appController = app.get<WebserverController>(WebserverController);
  // });
  //
  // describe('root', () => {
  //   it('should return "Hello World!"', () => {
  //     expect(appController.getHello()).toBe('Hello World!');
  //   });
  // });
});
